using UnityEngine;

/// <summary>
/// Base class for all abilities in the game
/// </summary>
public abstract class BaseAbility : MonoBehaviour
{
    // Base ability class with nothing in it, as requested
    // All ability logic is handled by child classes
}